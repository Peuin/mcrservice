# Example only — do NOT commit real secrets to Git.
# Create the real secret on the cluster with kubectl (see DEPLOY_ARGOCD.md).
apiVersion: v1
kind: Secret
metadata:
  name: worker-service-secret
  namespace: mcrservice
type: Opaque
stringData:
  SUPABASE_URL: "https://your-project.supabase.co"
  SUPABASE_SERVICE_ROLE_KEY: "your-service-role-key"
  OTP_HASH_SECRET: "your-otp-hash-secret"
