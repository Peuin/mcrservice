apiVersion: v1
kind: Service
metadata:
  name: worker-service
  namespace: mcrservice
  labels:
    app: worker-service
spec:
  type: ClusterIP
  selector:
    app: worker-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
