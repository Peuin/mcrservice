apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: worker-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Peuin/mcrservice.git
    targetRevision: main
    path: apps/worker-service/k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: mcrservice
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
